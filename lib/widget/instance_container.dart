import 'package:flutter/material.dart';

class InstanceContainer extends StatelessWidget {
  late final String domain;

  InstanceContainer(String d, {super.key}) {
    domain = d;
  }

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
