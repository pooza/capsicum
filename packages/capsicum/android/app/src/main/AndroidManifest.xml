<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- #1108: OAuth の認可を待つあいだ、プロセスを凍結させないための
         foreground service。⚠ shortService 型は型固有の権限を要求しないので、
         権限はこれ 1 つで足りる。
         ⚠⚠ Play Console への用途申告は、v1.64 の提出では求められなかった
         （2026-09-12 実測: 「アプリのコンテンツ」に項目が出ず、production への
         昇格も止められず公開された）。⚠ この件は「不要」→「必要」→「実測では
         求められなかった」と 2 回ひっくり返っているので、**次に触るときは実測を
         優先する**。型固有の権限（FOREGROUND_SERVICE_<型>）を足すと話が変わる。 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- メディアビューアのギャラリー保存 (#646、gal)。API 29+ は MediaStore
         経由で権限不要のため、28 以下に限定して宣言する。 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <application
        android:label="capsicum"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- capsicum は URL でルートを開かない。Flutter の deep link 処理
                 （3.27 以降は既定で有効）が capsicumauth:// の data を初期ルート
                 として go_router に渡すと、コールドスタートで splash / EULA を
                 飛ばして任意ホストのログイン画面を開けてしまう（v1.64 のリリース前
                 レビュー）。起動中の側は onNewIntent で中和している。 -->
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="false"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <!-- #276: ループバック OAuth の callback ページから MainActivity を
                 前面へ復帰させる専用スキーム。フォアグラウンドのブラウザが起動
                 するため background activity start 制限を受けない。スキーム/ホストは
                 constants.dart の androidOAuthReturnUrl と一致させること。 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="capsicumauth" android:host="complete"/>
            </intent-filter>
        </activity>
        <!-- #1108: OAuth の loopback callback を待つあいだだけ上げる keep-alive。
             ⚠ **凍結されると callback ページを返せない**ので、承認してもブラウザが
             戻ってこない。詳細は OAuthKeepAliveService の doc。
             ⚠ exported="false": 外から起動される必要は無い。 -->
        <service
            android:name=".OAuthKeepAliveService"
            android:exported="false"
            android:foregroundServiceType="shortService"/>
        <activity
            android:name="com.linusu.flutter_web_auth_2.CallbackActivity"
            android:exported="true">
            <intent-filter android:label="flutter_web_auth_2">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="capsicum" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
