name: fediverse_objects
description: API response DTOs for Mastodon and Misskey
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  json_annotation: ^4.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
  build_runner: ^2.4.15
  json_serializable: ^6.9.5
