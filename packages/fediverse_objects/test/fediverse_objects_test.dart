import 'package:test/test.dart';

void main() {
  group('fediverse_objects', () {
    test('package can be imported', () {
      // DTO classes will be tested here as they are added.
      expect(true, isTrue);
    });
  });
}
