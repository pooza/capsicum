name: capsicum_core
description: Domain models and adapter interfaces for capsicum
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies: {}

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
